<% provide(:title, "User Update" ) %>
<% provide(:url, user_path(@user) ) %>
<% provide(:button_text, "Update!" ) %>

<h1 class="h1s">User Edit</h1>

<div class="row"> 
  <div class="col-md-6 col-md-offset-3"> 
    <%= render 'form' %>
    <div class="gravatar_edit">
     <%= gravatar_for @user %>
     <a href="http://gravatar.com/emails" target="blank">Change</a>
    </div>
  </div>
</div>