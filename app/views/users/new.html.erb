<% provide(:title, "Sign up" ) %>
<% provide(:url, users_path ) %>
<% provide(:button_text, "登録" ) %>

<h1 class="h1s">Sign up</h1>

<div class="row"> 
  <div class="col-md-6 col-md-offset-3"> 
    <%= render 'form' %>
  </div>
</div>